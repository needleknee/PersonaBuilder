# Local LLM Configuration (Default)
# Uses OpenAI-compatible endpoint for extraction (llama.cpp, LM Studio, etc.)
LOCAL_LLM_BASE_URL=http://127.0.0.1:50305
LOCAL_LLM_MODEL=local-model

# OpenAI Configuration (Optional - for production fallback)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4-turbo

# Anthropic Configuration (Optional - for production fallback)
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-opus-20240229

# Environment
# NODE_ENV=development
# PORT=3000
